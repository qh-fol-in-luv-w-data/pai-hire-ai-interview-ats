const DB_NAME='PAI_HR_Interview'; const STORE_NAME='answers_store';
function openDb():Promise<IDBDatabase>{return new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,1);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains(STORE_NAME))request.result.createObjectStore(STORE_NAME)};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
export async function saveAnswer(key:string,blob:Blob){const db=await openDb();return new Promise<void>((resolve,reject)=>{const req=db.transaction(STORE_NAME,'readwrite').objectStore(STORE_NAME).put(blob,key);req.onsuccess=()=>resolve();req.onerror=()=>reject(req.error)})}
export async function readAnswer(key:string):Promise<Blob|null>{const db=await openDb();return new Promise(resolve=>{const req=db.transaction(STORE_NAME,'readonly').objectStore(STORE_NAME).get(key);req.onsuccess=()=>resolve(req.result||null);req.onerror=()=>resolve(null)})}
export async function clearAnswers(prefix:string){const db=await openDb();const tx=db.transaction(STORE_NAME,'readwrite');const store=tx.objectStore(STORE_NAME);return new Promise<void>(resolve=>{const req=store.getAllKeys();req.onsuccess=()=>{req.result.forEach(key=>typeof key==='string'&&key.startsWith(prefix)&&store.delete(key));tx.oncomplete=()=>resolve()};req.onerror=()=>resolve()})}
export const storagePrefix=(ref:string,position:string,prep:string,reiv:string)=>`ans_${ref||position}_${prep||reiv||'draft'}_`;
